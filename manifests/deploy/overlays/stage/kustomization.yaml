generatorOptions:
  disableNameSuffixHash: true
  
bases:
  - ../../base
  
namespace: {{NAMESPACE}}

nameSuffix: -{{ENV}}
commonLabels:
    app: {{PID-APP}}
    env: {{ENV}}

patchesStrategicMerge:
  - ingress-route.yaml
  - middleware.yaml
  - deployment.yaml
   